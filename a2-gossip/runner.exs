A2.gossip()

# {:ok, pid1} = Stack.start_link([])
# {:ok, pid2} = Stack.start_link([])

# Stack.push(pid1,pid2)
# Stack.push(pid1,pid2)
# top = Stack.pop(pid1)
# IO.inspect top

# top = Stack.pop(pid1)
# IO.inspect top

# # # GenServer.cast(pid, :increment)
# # # counter = GenServer.call(pid, :get_data)
# # # IO.inspect counter

    # {:ok, pid1} = Actor.start_link([])
    # {:ok, pid2} = Actor.start_link([])
    # {:ok, pid3} = Actor.start_link([])
    # {:ok, pid4} = Actor.start_link([])

    # Actor.push_pid(pid1,pid2)
    # Actor.push_pid(pid2,pid3)
    # Actor.push_pid(pid3,pid4)

    # top = Actor.gossip(pid1)
    # IO.inspect top

    